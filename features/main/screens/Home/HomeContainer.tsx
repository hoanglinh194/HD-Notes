import React, { useState } from "react";

import HomeView, { CreateStudiValues } from "./HomeView";

const CreateShudiContainer = () => {
  const [showModalSetting, setShowModalSetting] = useState(false);

  return (
    <HomeView
      // control={control}
      // isSubmitting={isSubmitting}
      // isShowModalSetting={showModalSetting}
      // isValid={isValid}
      // errors={errors}
      // onPressPhoto1={onPressPhoto1}
      // onPressPhoto2={onPressPhoto2}
      // onCloseModalSetting={onCloseModalSetting}
      // onPressModalSetting={onPressModalSetting}
      // handleSubmit={handleSubmit}
    />
  );
};

export default CreateShudiContainer;
